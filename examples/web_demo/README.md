# Web Demo

Single-page HTML client. Open it directly in a browser after starting any
VoxCore example. It talks to `/auth/register`, `/auth/login`, then `/ws`.

Audio capture is intentionally omitted — see the upcoming `sdks/js` package
for the full WebRTC-enabled client.
